# 2026-06-22 — Session Notes

## Telegram Reminders: Google Meet Link
- Added Meet link to 15-min reminders: `<Meeting Name> is starting in 15'\n<meet link>`
- Link pulled from Google Calendar event; cache self-heals every 15 min for events missing the field

## Post-Meeting Minutes: Full Automation
- `poll_happyscribe.py` runs every 5 min via cron — detects completed transcripts 30 min–2 hrs after meeting start
- No manual trigger needed; fully autonomous end-to-end

## Workflow Fixes & Decisions
- **PDF attachment bug**: MIME type corrected; email now sends a real `.pdf`, not a `.docx` renamed
- **HappyScribe export bug**: was calling direct export endpoint; now uses jobs API — required for polling to work
- **Template workflow**: download blank template from Drive, fill it locally (temp dir), PDF it, email it, upload to Drive, clean up. Never use a stale local copy.
- **Apologies field**: detect absentees by cross-referencing HappyScribe speaker list vs. Google Sheet roster
- **Claude auth**: uses OpenClaw's built-in auth; no separate ANTHROPIC_API_KEY in .env.intrastack

## Incident
- HappyScribe missed the Intrastack Website Project meeting today — no transcript captured, no minutes sent
- AI Meeting Transcriber Platform minutes initially sent as raw transcript (bad). Re-ran correctly after workflow fix.

## Fixes Applied (afternoon session)

- Tekstack Academy + Dragon POS minutes were stuck due to `claude` PATH bug in cron — fixed to use `/usr/local/bin/claude`
- CC stefan.nguyen@intrastack.com hardcoded permanently in `send_email.py`
- Name correction: fuzzy match threshold lowered 0.6 → 0.5; full roster passed to Claude summarize prompt (prevents Luan → Lorne type garbling)
- `~/.openclaw` added as additionalDirectory in global `~/.claude/settings.json` — no more permission prompts
